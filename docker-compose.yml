services:
  skycontrol:
    container_name: skycontrol
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    networks:
      - skycontrol-net

networks:
  skycontrol-net:
    driver: bridge
